endpoints:
  Tickets:
    GET:
      summary: "GET Tickets"
      description: "Handle GET requests for /{organisation}/tickets"
      tags:
        - Tickets
      ## uncomment this and the next line if this is a public endpoint
      #security:
      #  - {}
      ## comment if does not includes path params
      pathParams:
       - name: organisation
         description: Organisation slug
         schema:
           type: string
      ## uncomment if this is a POST request and define model
      #requestBody:
      #  description: "Description of the request body"
      #requestModels:
      #  application/json: "CreateTicketsRequest" # models defined in serverless.models.yml in top level for now
      methodResponses:
        - statusCode: 200
          responseBody:
            description: Success
        - statusCode: 405
          responseBody:
            description: Method Not Allowed
        - statusCode: 500
          responseBody:
            description: Internal Server Error
    POST:
      summary: "POST Tickets"
      description: "Handle POST requests for /{organisation}/tickets"
      tags:
        - Tickets
      ## uncomment this and the next line if this is a public endpoint
      #security:
      #  - {}
      ## comment if does not includes path params
      pathParams:
       - name: organisation
         description: Organisation slug
         schema:
           type: string
      ## uncomment if this is a POST request and define model
      #requestBody:
      #  description: "Description of the request body"
      #requestModels:
      #  application/json: "CreateTicketsRequest" # models defined in serverless.models.yml in top level for now
      methodResponses:
        - statusCode: 200
          responseBody:
            description: Success
        - statusCode: 405
          responseBody:
            description: Method Not Allowed
        - statusCode: 500
          responseBody:
            description: Internal Server Error
    PUT:
      summary: "PUT Tickets"
      description: "Handle PUT requests for /{organisation}/tickets"
      tags:
        - Tickets
      ## uncomment this and the next line if this is a public endpoint
      #security:
      #  - {}
      ## comment if does not includes path params
      pathParams:
       - name: organisation
         description: Organisation slug
         schema:
           type: string
      ## uncomment if this is a POST request and define model
      #requestBody:
      #  description: "Description of the request body"
      #requestModels:
      #  application/json: "CreateTicketsRequest" # models defined in serverless.models.yml in top level for now
      methodResponses:
        - statusCode: 200
          responseBody:
            description: Success
        - statusCode: 405
          responseBody:
            description: Method Not Allowed
        - statusCode: 500
          responseBody:
            description: Internal Server Error