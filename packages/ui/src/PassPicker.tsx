'use client';
import { EventModelType } from "@dance-engine/schemas/events";
import { OrganisationType } from "@dance-engine/schemas/organisation";

const PassPicker = ({ event: _event, org: _org }: { event: EventModelType, org: OrganisationType }) => {
  // Legacy placeholder: this component currently lives in app code, not the shared UI package.
  return null;
};

export default PassPicker