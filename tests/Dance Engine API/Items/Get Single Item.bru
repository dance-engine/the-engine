meta {
  name: Get Single Item
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/{{currentOrg}}/{{currentEvent}}/items/:ksuid
  body: none
  auth: bearer
}

params:path {
  organisation: 
  event: 
  ksuid: 
}

auth:bearer {
  token: {{bearerToken}}
}
